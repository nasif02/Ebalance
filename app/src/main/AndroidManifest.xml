<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aaa.xplo.ebalance">


    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

    <activity
        android:name=".MainActivity"
        android:label="@string/app_name" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
        android:name=".SingleSim"
        android:label="@string/title_activity_single_sim"
        android:screenOrientation="portrait" >
    </activity>
    <activity
        android:name=".DualSim"
        android:label="@string/title_activity_dual_sim"
        android:screenOrientation="portrait" >
    </activity>
    <activity
        android:name=".SimConfig"
        android:label="@string/title_activity_sim_config"
        android:screenOrientation="portrait" >
    </activity>
</application>

</manifest>
